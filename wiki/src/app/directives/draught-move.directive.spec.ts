import { DraughtMoveDirective } from './draught-move.directive';

describe('DraughtMoveDirective', () => {
  it('should create an instance', () => {
    const directive = new DraughtMoveDirective();
    expect(directive).toBeTruthy();
  });
});
